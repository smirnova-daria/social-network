const initialState = {
  friends: [
    { id: 1, name: "Elena" },
    { id: 2, name: "Masha" },
    { id: 3, name: "Maxim" },
  ],
};

const navbarReducer = (state = initialState, action) => {
  return state;
};

export default navbarReducer;
